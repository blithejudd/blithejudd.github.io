# Состояние — 2026-09-19

## Реализовано

- Существующий репозиторий клонирован в `D:\blithejudd.github.io`; `D:\alex` не изменялся.
- Новый светлый editorial-дизайн, грузинское портфолио, адаптивная галерея, фильтры, dialog/клавиши/свайпы.
- Шесть уникальных фото вместо дубля, сохранены оригиналы, добавлены оптимизированные превью.
- Русская админка: настройка, login/logout, проверка владельца, очередь, WebP, метаданные/категории/порядок, черновики, удаление.
- Supabase REST без SDK; SQL с приватным списком владельцев, RLS, bucket и квотами формы.
- Telegram-токен удалён из браузерного кода; серверная Edge Function подготовлена. Старый токен требуется отозвать у BotFather, он остаётся в истории Git.
- Подробная инструкция подключения, preview-сервер и тесты без npm-зависимостей.

## Подключение Supabase и публикация

- В `js/config.js` добавлены URL проекта `tpbccsvlahbwupntcogq` и публичный Publishable key. Форма контакта остаётся выключенной. Секретный ключ не сохранялся и не использовался; раскрытый в переписке ключ необходимо отозвать в Dashboard.
- Через Management API выполнен `supabase/schema.sql` после проверки пустого проекта (0 таблиц public, bucket, политик и пользователей). Созданы 4 таблицы с RLS, 5 политик и bucket `portfolio`; 6 опубликованных записей доступны реальному публичному API.
- Публичная регистрация отключена (`disable_signup: true`), Site URL — `https://gulievi.me`.
- Пользователей и владельцев пока нет: ожидается email владельца. Реальный вход владельца, загрузка/удаление от его имени и доступ не-владельца ещё не проверены.
- Management token хранится только в локальных игнорируемых `.env.management` / `.env.management.txt`, не выводился в журнал. После настройки его следует отозвать.
- Edge Function не развёрнута, Telegram выключен; Instagram доступен. Старый Telegram-токен и раскрытый секрет Supabase требуют отзыва.
- По разрешению пользователя коммит `e10472b` отправлен в `origin/main`. GitHub Pages run `35463158048` завершён успешно; новая версия доступна на `https://gulievi.me/`, панель — `https://gulievi.me/admin/`. Цены и новые грузинские тексты требуют проверки владельцем.
- Hero/about и тексты страницы статичны; панель управляет галереей, не всем контентом.
- Черновики скрыты только из галереи. Bucket публичный; это не приватная клиентская фотогалерея.
- Нет CAPTCHA, email-reset UI, MFA UI и атомарной транзакции между Storage и БД. Сетевые неопределённости объясняются в панели/README; возможна ручная очистка orphan-файлов.

## Проверки

- `scripts/verify.ps1`: синтаксис JS, 10/10 unit-тестов, `git diff --check` — пройдены.
- Повторный прогон после подключения публичной конфигурации: 10/10 unit-тестов и `ALL BROWSER TESTS PASSED`. Тест неподключённого API и браузерный setup-сценарий изолированы от реальной конфигурации; браузерные операции записи используют только mock API.
- Chrome headless/CDP: desktop 1440 и ширины 320/390/768, фильтры, empty state, клавиатура лайтбокса, меню, guard неподключённой админки — пройдены.
- Mock API в настоящем браузере: отказ не-владельцу, вход владельца, редактирование, защита от HTML-инъекции, Canvas/WebP upload, черновик, отмена/подтверждение удаления, logout, форма контакта, сбой/повтор API, пустая БД без fallback — пройдены.
- Исправлена найденная браузерным тестом ошибка проверки корня preview-сервера на Windows.
- Скриншоты desktop/mobile/admin — в локальной игнорируемой папке `test-results/`.
- `tests/live-public.mjs`: реальный Auth и 6 записей с грузинскими подписями; анонимные insert/update/delete, чтение владельцев, вызов квоты отклонены (401), Storage upload отклонён (403). Тесты используют отдельные случайные probe IDs, не меняют существующие фото.
- SQL выполнен в облачном PostgreSQL через Management API. Deno локально отсутствует; Edge Function не исполнялась.
- `tests/browser.mjs --live`: дополнительно пройдены загрузка реальной галереи Supabase и доступность настроенной формы входа; вход владельца не имитируется как live-проверка.
- `scripts/check-deployment.mjs 35463158048`: подтверждены успешный Pages deploy и совпадение 10 опубликованных ресурсов с локальными файлами; оба локальных файла Management token возвращают 404. `DEPLOYMENT VERIFIED`.